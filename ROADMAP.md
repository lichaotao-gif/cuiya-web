# 项目路线

## 第二阶段：混合亮色主题

状态：已确认，暂不纳入当前部署版本。

### 目标

在保留现有深色沉浸式品牌风格的基础上，增加适合日间、办公和教育场景使用的混合亮色主题。

### 主题模式

网站需要提供三种选择：

1. 跟随系统
2. 浅色模式
3. 深色模式

默认选择“跟随系统”。用户手动选择后，将偏好保存在浏览器本地；用户选择“跟随系统”时，需要继续响应操作系统主题变化。

### 视觉原则

- 不采用全站简单反色。
- 首页首屏、产品首屏及其他图片型沉浸区域可以继续使用深色蒙层和浅色文字。
- 普通内容区、新闻、政策、下载、卡片和列表切换为浅色背景与深色正文。
- 根据主题自动使用 `logo-on-dark.svg` 或 `logo-on-light.svg`。
- 保留品牌橙色和蓝色辅助色，但分别校验亮色、暗色背景下的对比度。
- 卡片、边框、阴影、玻璃效果和图片蒙层需要逐组件适配，不能仅覆盖基础颜色变量。

### 交互要求

- 桌面端在顶部导航提供清晰的主题切换入口。
- 移动端在菜单中提供“跟随系统 / 浅色 / 深色”文字选项。
- 页面加载前确定主题，避免出现从深色闪到浅色的闪屏。
- 切换主题时不刷新页面，并尊重 `prefers-reduced-motion`。
- 切换控件需支持键盘操作，并提供明确的无障碍名称和当前状态。

### 技术建议

- 在 `<html>` 上使用 `data-theme="light"` 或 `data-theme="dark"`。
- 使用 `prefers-color-scheme` 获取系统主题。
- 使用 `localStorage` 保存手动选择；未设置时保持系统模式。
- 在页面 `<head>` 中运行最小化的主题初始化脚本，避免首屏闪烁。
- 为浏览器地址栏同步更新 `theme-color`。
- 先补充亮色设计变量，再针对图片 Hero、导航、卡片、研究院流程和文章正文增加组件级覆盖。

### 验收标准

- 首页、关于我们、研究院、四个公开产品页、新闻、政策、下载和 404 页面均完成亮暗主题检查。
- 320px、375px、768px、1024px 和 1440px 下无横向溢出或布局跳动。
- 主题选择能够跨页面和跨会话保持。
- 系统主题变化时，“跟随系统”模式能够即时响应。
- 文本及交互控件达到 WCAG AA 对比度要求。
- 页面刷新时无明显主题闪烁。
- 图片、二维码、视频控件、焦点状态和加载骨架在两种主题下均清晰可用。

